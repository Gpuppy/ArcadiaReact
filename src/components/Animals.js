import React, {useState, useEffect} from "react";

function Animals()
{
    const [useData, setAnimalData] = useState([]);

    return(
        <React.Fragment>
            <h5>Animals</h5>
        </React.Fragment>

    );
}
export default Animals;